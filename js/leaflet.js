"use strict";

// ---------- //
// Map Functions
// ---------- //

// Get zoom level
export function getZoomLevel(map) {
    map.getZoom();
};

// Set zoom level
export function setZoom(map, zoom) {
    map.setZoom(zoom);
};
